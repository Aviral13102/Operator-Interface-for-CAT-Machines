# Member 1 — Core Shell & Real-Time Data Layer
# Local dependencies for mock_server.py (replays telemetry over WebSocket)
fastapi==0.115.0
uvicorn[standard]==0.30.6
websockets==13.1
pydantic==2.9.2
python-dotenv==1.0.1
